import {
    StyleSheet,
} from 'react-native';
import { baseFontSize } from './baseUnits';

import { styles } from './baseStyles';

export const style = StyleSheet.create({
    webViewContainer: {
        flex: 1,
        margin: 0,
        padding: 0
    }
});